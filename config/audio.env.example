# AUTO ігнорує auto_null та службові шини. Автовибір не надає переваги USB/I2S/PCI:
# спочатку використовується активний фізичний sink, інакше стабільний fallback за назвою.
# Явний вибір з Web UI зберігається окремо у /var/lib/proaudio-player-alert/audio-output.env.
PHYSICAL_SINK=AUTO

# Внутрішній mix/limiter domain має стабільний processing rate. PipeWire виконує
# необхідний boundary resampling для конкретного фізичного пристрою. Це навмисно:
# перемикання graph rate під час активного Spotify/AirPlay/DLNA могло б рвати transport.
SAMPLE_RATE=48000
AUDIO_CHANNELS=2

MUSIC_SINK=proaudio_player_music
ALERT_SINK=proaudio_player_alert
MASTER_SINK=proaudio_player_master
LOOPBACK_LATENCY_MSEC=100

# Фізичний PipeWire/Pulse sink завжди unity. Користувацька MASTER-гучність живе
# тільки на MASTER_SINK перед limiter. Значення, відмінне від 100, вважається помилкою.
OUTPUT_VOLUME_PERCENT=100

# unity: ініціалізувати достовірні ALSA playback-контроли біля 0 dB без positive gain.
# capture/input/boost/gain/tone та контроли без dB-шкали не змінюються.
# Якщо безпечний <= 0 dB рівень не підтверджено, початковий raw-рівень відновлюється.
# off: взагалі не торкатися апаратного ALSA mixer.
HARDWARE_MIXER_MODE=unity

# Фінальний linked safety limiter після MASTER. Він не має input/makeup gain,
# ніколи не підсилює сигнал і оцінює intersample peak через polyphase FIR.
LIMITER_ENABLED=true
LIMITER_CEILING_DB=-1.0
LIMITER_LOOKAHEAD_MS=5.0
LIMITER_RELEASE_MS=100.0
LIMITER_OVERSAMPLE=4

SINK_WAIT_SECONDS=30

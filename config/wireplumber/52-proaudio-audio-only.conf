# ProAudio Player is an audio-only appliance. Camera discovery is not part
# of the runtime contract and should not delay or pollute session-manager
# startup on systems without video SPA plugins.
wireplumber.profiles = {
  main = {
    monitor.v4l2 = disabled
    monitor.libcamera = disabled
  }
}
